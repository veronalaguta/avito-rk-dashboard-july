# Дашборд Avito РК: проектная документация

## Цель проекта

Дашборд нужен, чтобы оценивать эффективность рекламных кампаний на Авито по профилям, направлениям, городам, кампаниям и креативам.

Основные бизнес-задачи:
- понять, какие рекламные кампании дают заказы;
- контролировать цену заказа;
- видеть, какие направления и города запущены в каждом профиле;
- находить слабые связки профиль / направление / город;
- формировать гипотезы для оптимизации;
- отдельно анализировать объявления, заголовки, тексты, цены и превью.

## Публичная ссылка

https://veronalaguta.github.io/avito-rk-dashboard-july/

## Репозиторий

https://github.com/veronalaguta/avito-rk-dashboard-july

## Период данных

Текущий период анализа: с 2026-07-01 по 2026-07-23.

## Источники данных

### Основная KPI-таблица

Файл:
`/Users/veronikalagutkina/Downloads/Кефир. Вероника. Основная таблица. Условия работы - июль.csv`

Используется как основной источник по эффективности РК:
- профиль;
- направление;
- город;
- дата;
- расходы;
- заказы;
- цена заказа;
- звонки;
- целевые звонки;
- просмотры и контакты, если есть в выгрузке.

Это основной источник правды для финальной эффективности: заказы, цена заказа, расходы.

### Выгрузки Avito Pro

Файлы:
- `/Users/veronikalagutkina/Downloads/Геннадий Статистика_с_2026-07-01_по_2026-07-23.xlsx`
- `/Users/veronikalagutkina/Downloads/Киякин Денис Статистика_с_2026-07-01_по_2026-07-23.xlsx`
- `/Users/veronikalagutkina/Downloads/Матросов Статистика_с_2026-07-01_по_2026-07-23.xlsx`
- `/Users/veronikalagutkina/Downloads/Орлов Статистика_с_2026-07-01_по_2026-07-23.xlsx`

Используются для статистики по объявлениям за период:
- показы;
- просмотры;
- контакты;
- чаты;
- просмотры телефона;
- избранное;
- расход;
- средняя цена контакта.

Важная техническая деталь: `Номер объявления` в Excel хранится как формула `HYPERLINK(...)`. При обычном импорте через `pandas` поле может выглядеть пустым. Нужно читать файл через `openpyxl` с `data_only=False` и доставать ID объявления из формулы.

### Google Sheets из Avito XML-бота

Таблицы:
- Киякин Денис: https://docs.google.com/spreadsheets/d/1411aUKIYUGbVtkaJTlKePnddSziFG_W4kZG7KzY3G_E/edit
- Александр Матросов: https://docs.google.com/spreadsheets/d/1qYl6lem9shbLfGXhzDlaptVZwXSTklN5XQxJd-Gmf9k/edit
- Геннадий Сергеевич: https://docs.google.com/spreadsheets/d/1ELz-6Kd3JdDdKUfpLiJvZHtiMEGh42GDQRcZTkeDk-E/edit
- Николай Орлов: https://docs.google.com/spreadsheets/d/1vIDjZ4PrJwQtrFkmJ0gyKv_19tRq3hQqUUylo4FjG1A/edit

Используются как справочник состава объявлений:
- ID объявления;
- ссылка на Авито;
- заголовок;
- текст;
- цена;
- ссылки на изображения;
- первое превью;
- папка / набор картинок;
- город, адрес, метро;
- категория / направление;
- статус и даты.

Эти таблицы не считаем основным источником статистики по эффективности. Они нужны, чтобы объяснить, из чего состояло объявление.

## Правила анализа метрик

### Эффективность РК

Главный KPI: заказ.

Дополнительные метрики:
- количество заказов;
- цена заказа;
- расход;
- звонки;
- целевые звонки;
- контакты;
- покрытие по направлениям и городам.

Рабочие ориентиры:
- электрика и сантехника: цена заказа до 1 200 руб.;
- бытовая техника: допустимый ориентир выше, в текущем дашборде используется 1 800 руб. как рабочий порог до уточнения.

### Анализ объявления по этапам воронки

Анализ аккаунта нужно раскладывать по составляющим объявления, а не смешивать все в один вывод.

Первый этап: превью.

Превью = первое фото объявления.

Для превью смотрим:
- CTR превью = `просмотры / показы`.
- показы;
- просмотры;
- контакты;
- CR в контакт;
- цену контакта.

CTR показывает прямой вход из показа в просмотр. Контакты рядом нужны, чтобы понимать, как в целом отрабатывают объявления с этим первым фото.

Второй этап: связки объявления.

Связка = город + первое фото + заголовок + текст.

Для связок смотрим:
- показы;
- просмотры;
- CTR;
- контакты;
- CR в контакт;
- цену контакта.

Этот слой нужен, чтобы отделять эффект первого фото от эффекта города, заголовка и текста.

Правило интерпретации:
- высокий CTR и высокая контактность = кандидат на масштабирование;
- высокий CTR и низкая контактность = картинка цепляет, но нужно проверять заголовок, текст, цену, оффер или гео;
- низкий CTR и высокая контактность = объявление убеждает тех, кто открыл, но превью может недобирать входящий трафик;
- низкий CTR и низкая контактность = кандидат на переработку или остановку.

### Анализ объявления целиком

Для отдельного анализа объявлений можно использовать полную воронку:
- показы;
- просмотры;
- контакты;
- чаты;
- звонки / просмотры телефона;
- заказы;
- расход;
- цена контакта;
- цена заказа.

Этот блок должен быть отдельным от анализа превью.

### Диагностика проблем по воронке

Дашборд должен помогать не только смотреть метрики, но и быстро понимать, где именно сломалась связка.

Базовая логика:
- много показов, низкий CTR = проблема на входе: первое превью, заголовок, релевантность услуги, город, конкуренция в выдаче;
- высокий CTR, мало контактов = картинка привела людей внутрь, но связка объявление / цена / текст / оффер / гео не довела до обращения;
- контакты есть, заказов мало = проблема ниже объявления: качество лида, обработка, скорость ответа, дозвон, цена после консультации;
- низкий CTR, высокая контактность = объявление хорошо убеждает тех, кто открыл, но ему нужно улучшить входящий поток через превью или заголовок;
- высокая цена контакта = нужно разделять дорогой аукцион, слабый CTR и слабую конверсию в контакт;
- нормальный CPO и стабильные заказы = кандидат на масштабирование.

Гипотезы формируем по шаблону:
- наблюдение: что видим в данных;
- возможная причина: какой элемент воронки может влиять;
- тест: что меняем;
- критерий успеха: какая метрика должна измениться;
- ограничение: что может исказить вывод.

Пример:
- наблюдение: CTR высокий, но CR в контакт низкий;
- возможная причина: превью цепляет, но текст или цена не совпадают с ожиданием;
- тест: оставить первое превью, поменять заголовок, первый абзац и оффер;
- критерий успеха: рост CR в контакт без падения CTR;
- ограничение: тестировать в том же городе и направлении, иначе будет мешать спрос.

## Структура дашборда

### Вкладка `Обзор`

Высокоуровневый анализ:
- основные KPI;
- заказы и цена заказа по направлениям;
- заказы и цена заказа по профилям;
- динамика по дням;
- гипотезы для оптимизации;
- методология диагностики воронки;
- покрытие запусков;
- проблемные связки профиль / направление / город.

### Вкладка `Аккаунты`

Глубокий анализ по конкретному профилю.

Подключенные профили:
- Николай Орлов;
- Геннадий Сергеевич;
- Александр Матросов;
- Киякин Денис.

Планируемые блоки:
- анализ превью как первого фото;
- анализ связок город + превью + заголовок + текст;
- топ объявлений с полной воронкой;
- анализ заголовков;
- анализ текстов и офферов;
- анализ цены;
- анализ наборов изображений;
- анализ гео / метро;
- диагностика воронки.

## Текущая реализация

Дашборд сделан как статический сайт GitHub Pages:
- основной файл: `index.html`;
- подготовленные данные по объявлениям: `data/processed/creative_accounts.json` и `data/processed/creative_accounts.js`;
- локальные первые превью: `assets/*-all-previews/`.

Сейчас есть:
- вкладка `Обзор`;
- вкладка `Аккаунты`;
- блок всех объявлений по выбранному профилю;
- рекомендации по связкам;
- агрегирование по одинаковому первому превью;
- связки `город + превью + заголовок + текст`.

Картинки превью скачаны локально, чтобы они стабильно открывались на GitHub Pages и не зависели от внешних `http://` ссылок.

## Принципы масштабирования

Проект нужно развивать так, чтобы каждый новый аккаунт, период или блок анализа добавлялся через данные и понятный пайплайн, а не через ручную правку большого HTML.

Целевая структура данных:
- сырые выгрузки Avito Pro хранятся отдельно по аккаунтам и периодам;
- Google Sheets используются как справочник состава объявлений;
- после обработки формируется единый нормализованный датасет;
- дашборд читает уже подготовленные JSON-файлы;
- картинки превью хранятся локально и связаны с объявлениями через ID или hash.

Желаемая структура проекта:
- `data/raw/` — исходные выгрузки, если решим хранить их в репозитории или локально рядом с проектом;
- `data/processed/` — подготовленные JSON/CSV для дашборда;
- `assets/previews/` — скачанные превью;
- `scripts/` — скрипты импорта, очистки, матчинга и генерации данных;
- `docs/` — логика проекта, правила анализа, история решений;
- `index.html` — только интерфейс и визуализация.

Чтобы проект было легко дорабатывать, каждую новую сущность добавляем по одному и тому же шаблону:
- новый аккаунт = новая запись в конфиге аккаунтов;
- новый период = новая папка или параметр периода;
- новый источник данных = отдельный шаг обработки;
- новый аналитический блок = отдельная функция расчета и отдельный блок интерфейса;
- новая гипотеза = фиксируется в логе или отдельном списке гипотез.

## Правила доработок

При изменениях важно не смешивать разные уровни анализа:
- РК оцениваем по заказам, цене заказа, расходу и покрытию;
- превью оцениваем по CTR, а контакты рядом используем как оценку связки после просмотра;
- объявление целиком оцениваем по полной воронке;
- текст, заголовок, цена и оффер анализируются отдельно от картинки.

Каждое новое допущение нужно фиксировать в документации:
- почему выбрана метрика;
- какой источник считается главным;
- какие данные пока неполные;
- какие выводы являются гипотезами, а не доказанными правилами.

Каждую крупную итерацию нужно добавлять в `docs/project-log.md`:
- что изменили;
- какие данные подключили;
- какие решения приняли;
- какие ограничения остались;
- что делать следующим шагом.

## Текущая версия

Собран единый сгенерированный датасет по четырем профилям:
- Николай Орлов ИП: 166 сервисных объявлений в таблице, 166 с первым фото, 30 групп с картинкой;
- Геннадий Сергеевич ИП: 149 сервисных объявлений в таблице, 69 с первым фото, 35 групп с картинкой;
- Матросов Александр: 21 сервисное объявление в таблице, 19 с первым фото, 2 группы с картинкой;
- Киякин Денис: 79 сервисных объявлений в таблице, 72 с первым фото, 4 группы с картинкой.

Во вкладке `Аккаунты` есть три уровня анализа:
- `Все объявления профиля` — мини-таблица по всем сервисным объявлениям выбранного профиля с превью, ID, заголовком, городом, датой/временем публикации, днями на Авито, показами, CTR, просмотрами, CR в контакт, контактами, ценой просмотра, ценой контакта и расходом;
- `Рекомендации по связкам` — три решения: масштабировать, наблюдать, заменить; в карточках есть превью и номера объявлений;
- `Превью: первое фото` — агрегирование по одинаковому первому изображению;
- `Связки: город + превью + заголовок + текст` — разбор полной связки объявления.

Группировка первых фото идет по хэшу скачанного файла. Визуальный hash сохраняется как метаданные, но не используется для группировки, потому что он может склеивать похожие, но не одинаковые изображения.

## Ограничения текущей версии

- У Геннадия, Матросова и Киякина часть объявлений из Avito Pro не сматчена с XML-таблицами. Метрики по ним есть в таблице объявлений, но превью/текст появляются только там, где ID найден в XML.
- Объявления из `Запчасти и аксессуары` скрываются из сервисной таблицы, потому что они не относятся к анализу услуг.
- Дашборд пока статический и не обновляется сам после новой выгрузки Avito Pro.

## Следующие шаги

1. Разобраться с несматченными ID по Геннадию, Матросову и Киякину: проверить, есть ли они в других вкладках XML-таблиц или это старые/неуслуговые объявления.

2. Добавить ручной список объявлений с накруткой/искажением, чтобы исключать их из benchmark-выводов.

3. Добавить отдельный анализ заголовков, текстов, цен и офферов.

4. Связать объявления/РК с заказами из основной таблицы, чтобы смотреть не только контакты, но и путь до заказа.

5. Позже автоматизировать обновление после добавления новых выгрузок Avito Pro в папку.
